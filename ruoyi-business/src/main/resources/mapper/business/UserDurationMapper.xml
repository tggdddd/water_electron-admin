<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.business.mapper.UserDurationMapper">
    
    <resultMap type="UserDuration" id="UserDurationResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="expireTime"    column="expire_time"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectUserDurationVo">
        select id, user_id, expire_time, create_by, create_time, update_by, update_time from user_duration
    </sql>

    <select id="selectUserDurationList" parameterType="UserDuration" resultMap="UserDurationResult">
        <include refid="selectUserDurationVo"/>
        <where>  
            <if test="params.beginExpireTime != null and params.beginExpireTime != '' and params.endExpireTime != null and params.endExpireTime != ''"> and expire_time between #{params.beginExpireTime} and #{params.endExpireTime}</if>
        </where>
    </select>
    
    <select id="selectUserDurationById" parameterType="Long" resultMap="UserDurationResult">
        <include refid="selectUserDurationVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertUserDuration" parameterType="UserDuration" useGeneratedKeys="true" keyProperty="id">
        insert into user_duration
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="expireTime != null">expire_time,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="expireTime != null">#{expireTime},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateUserDuration" parameterType="UserDuration">
        update user_duration
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteUserDurationById" parameterType="Long">
        delete from user_duration where id = #{id}
    </delete>

    <delete id="deleteUserDurationByIds" parameterType="String">
        delete from user_duration where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>